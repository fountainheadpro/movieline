

/** @jsx React.DOM */

/*
  var SearchBar = React.createClass({
    getInitialState: function() {
      return {
        prompt: "Zed's dead",
        query: ""
      };
    },    

    handleChange: function() {
        //this.props.onUserInput(
        //    this.refs.filterTextInput.getDOMNode().value,            
        //);
    },    

    render: function() {
        return (
            <form>
                <input 
                  type="text" 
                  class="searchBar" 
                  placeholder={this.props.prompt} 
                  ref="filterTextInput"
                  value={this.props.query} />
            </form>
        );
    }
  });

var Scene=React.createClass({
  render: function(){
    return(
        <a href="#" class="thumbnail">
          <img data-src="{this.props.scene.imageSource}" alt="{this.props.scene.caption}"/>
          <p>{this.props.scene.caption}</p>
        </a>      
    )    
  }
});  


var Clip = React.createClass({
    render: function() {
      var scope = this.props.scope;
      var images=_.map(scope.data, function(scene){
        return <li class="span4"><Scene scene={scene} key={scene.key}/></li>
      })
      <ul class="thumbnails">
        {images}
      </ul>
    }
});

var clip = [
  {imageSource: 'http://anim.livelin.es/animation_1563_1.gif', key: 1563, "Zed's dead, baby. Zed's dead."},
  {imageSource: 'http://anim.livelin.es/animation_1562_1.gif', key: 1562, "Zed's dead, baby. Zed's dead."},
  {imageSource: 'http://anim.livelin.es/animation_1561_1.gif', key: 1561, "Zed's dead, baby. Zed's dead."},
  {imageSource: 'http://anim.livelin.es/animation_1560_1.gif', key: 1560, "Zed's dead, baby. Zed's dead."}
];

*/
